package bonch.dev.school.ui.activities

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import bonch.dev.school.R
import bonch.dev.school.ui.fragments.ChatFragment
import bonch.dev.school.ui.fragments.ProfileFragment

class MainAppActivity : AppCompatActivity() {
/*
    val fm = supportFragmentManager

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.main_app_activity)

        val chatFragment = ChatFragment()
        fm.beginTransaction()
            .add(R.id.main_app_fragment, chatFragment)
            .commit()
    }

    fun replaceFragment(){
        val profileFragment = ProfileFragment()
        fm.beginTransaction()
            .replace(R.id.main_app_fragment, profileFragment)
            .addToBackStack("profile_fragment")
            .commit()
    }


 */
}
